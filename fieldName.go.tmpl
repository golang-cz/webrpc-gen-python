{{- define "fieldName" -}}
{{- $field := .Field -}}
{{- $fieldName := $field.Name -}}

{{- range $meta := $field.Meta -}}
    {{- if eq $meta.Name "json" -}}
        {{- $fieldName = $meta.Value | split "," | first -}}
    {{- end -}}
{{- end -}}

{{- $fieldName -}}
{{- end -}}
